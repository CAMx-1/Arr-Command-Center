node_modules/
config.json
.env
.auth-state.json
*.log
.DS_Store

# App data store (contains captured Plex token)
data/

# Local audit/regression tests
/test/
/server/test/

# Capacitor / iOS — the native project is committed, but not build output,
# CocoaPods, or the synced web-asset copy (regenerated by `npx cap sync ios`).
ios/App/Pods/
ios/App/App/public/
ios/App/build/
ios/App/output/
ios/DerivedData/
ios/**/ModuleCache.noindex/
DerivedData/
xcuserdata/
*.xcuserstate
